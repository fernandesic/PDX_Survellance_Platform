# Baseline — 2026-06-08

Captured at start of Phase 0 cleanup. This is what we must not regress.

## Backend (`backend-main/`, venv active)

- `python manage.py check` → **PASS** (1 warning: `staticfiles.W004` — `static/` dir missing)
- `python manage.py test --noinput` → **FAIL** — test DB setup errors out during migration apply with `django.db.utils.ProgrammingError: column "tenant_id" does not exist`. No tests run. Indicates RawSQL/RunSQL migration referencing `tenant_id` runs before the column exists on a fresh DB (migration order issue). Must be fixed before backend tests can produce a meaningful baseline.

## Frontend (`frontend-main/`)

- `npm run lint` → **FAIL** — 1141 problems (1091 errors, 50 warnings). Dominant rules: `@typescript-eslint/no-explicit-any`, `no-unused-vars`, `no-useless-escape`, unused eslint-disable directives.
- `npx tsc -b --noEmit` → **FAIL** — 1 error: `src/pages/poe/services/poe.ts(3,25)` `TS1484` (must use type-only import for `ArcGISFeature` under `verbatimModuleSyntax`).
- `npm run build` → **FAIL** — same TS1484 error blocks `tsc -b` step before `vite build`.
- `npm run test` (vitest) → **FAIL** — 9 failed / 295 passed / 7 skipped across 3 failing files (21 total).

## What this means for the cleanup

- Frontend baseline is already red on build/lint/tsc/test. Each Phase 1/2 task must
  be measured against these counts, not "pass/fail," to prove progress.
- Backend baseline is green on `check`; test result pending.
